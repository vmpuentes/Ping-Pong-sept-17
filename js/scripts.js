//Business Logic

var pingPongArray = ["Ping", "Pong", "PING-PONG"];
var outputNumber =
var outputArray = [];
for (var i = 0, i<= (pingPong.length), i ++ )
  if (i % 3 === 0) {
    $(outputArray).append('<img src="img/gump.png">PING-PONG!').hide().fadein(200);
  else if (i % 5 === 0) {
    $(outputArray).append();
  } else if (i % 15 === 0) {
    $(outputArray).append();
  }
}
}

event.preventDefault();
return outputArray;
}


//User Logic
$(document).ready(function() {
  $("#formOne").submit(function(event) {
    var userInput = parseInt($("#numberInput").val());
    console.log(userInput);

  }
}
